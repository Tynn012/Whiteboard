# Optional heavy dependencies for local LLM mode
# Note: Installing torch/torchvision may require platform-specific wheels (CUDA). Adjust accordingly.
transformers>=4.41.0
torch>=2.0.0
torchvision>=0.15.0
accelerate>=0.20.0
bitsandbytes>=0.39.0; platform_machine!='aarch64'
huggingface_hub>=0.16.4
sentencepiece>=0.2.0

# Install with:
# python -m pip install -r requirements-llm.txt
