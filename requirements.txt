streamlit>=1.33
datasets>=2.14.0
pandas>=2.0.0

# NOTE: heavy ML dependencies (transformers/torch/accelerate/bitsandbytes)
# were moved to requirements-llm.txt to avoid failing Streamlit builds.
# To install LLM-capable deps, run:
# python -m pip install -r requirements-llm.txt
# (no requests needed — LLM removed)
